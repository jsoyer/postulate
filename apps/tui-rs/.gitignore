/target
# Cargo.lock is intentionally committed: tui-rs is a binary/app (not a
# library), so its lockfile is tracked for reproducible CI builds
# (`cargo build --locked` / `cargo test --locked`).

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
