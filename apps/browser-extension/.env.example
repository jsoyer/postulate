# cv-browser-extension (MV3 Chrome/Firefox)
#
# The extension stores its API URL and API key in extension storage at runtime
# (entered by the user in the options page). There are no build-time secrets.
#
# Default API URL used until the user configures one (see src/lib/constants.ts).
DEFAULT_API_URL=http://localhost:8080
